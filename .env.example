# Seats.aero Partner API key (required). Get one from your Seats.aero Partner account.
SEATS_AERO_API_KEY=

# Gmail app password for SMTP email notifications (optional — only needed if you
# use the built-in email notifier rather than routing hits through your agent).
AWARD_SMTP_PASSWORD=
