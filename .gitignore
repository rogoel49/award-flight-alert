# Local, machine-specific data — NEVER commit these to a public repo.
# Your alert list, real config, secrets, and dedupe state stay on your machine only.
config.json
alerts.json
.env
state.json
new_hits.json
.poll-host
*.lock
*.log

# Python
__pycache__/
*.pyc
*.pyo

# Editor / OS
.DS_Store
.idea/
.vscode/
